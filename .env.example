# Mashriq Editions — environment variable template (placeholders only).
# Real secrets live in the Vercel and Supabase dashboards — NEVER commit actual values.

# Supabase backend (required)
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Higgsfield media generation (future)
# HIGGSFIELD_API_KEY=

# Stripe checkout (future)
# STRIPE_PUBLISHABLE_KEY=
# STRIPE_SECRET_KEY=
